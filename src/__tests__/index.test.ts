// Happy Path
// Transitions work
// close callback works
// prop variations don't blow shit up

describe('Simple Thing', () => {
  it('should just work', () => {
    expect(true).toBe(true);
  });
});
